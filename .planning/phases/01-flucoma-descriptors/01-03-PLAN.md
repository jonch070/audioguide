---
phase: 01-flucoma-descriptors
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - audioguide/tests.py
  - audioguide/anallinkage.py
autonomous: false

must_haves:
  truths:
    - "User can configure FLUCOMA_ENABLE=True and FLUCOMA_DESCRIPTORS=['mfcc'] and get MFCC values"
    - "Preset FLUCOMA_PRESET='timbre' works without listing individual descriptors"
    - "Corpus descriptors cached, target descriptors computed fresh"
  artifacts:
    - path: "audioguide/tests.py"
      provides: "FLUCOMA_* option validation"
      contains: "FLUCOMA_ENABLE"
  key_links:
    - from: "config"
      to: "FluCoMaBackend"
      via: "anallinkage.py descriptor resolution"
      pattern: "FLUCOMA.*anallinkage.*descriptor"
---

<objective>
Finalize integration and add validation tests for FluCoMa descriptor options.

Purpose: Ensure all FLUCOMA_* options are validated and the full pipeline works end-to-end
Output: Tests for option validation, verification script for end-to-end testing
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-flucoma-descriptors/01-RESEARCH.md
@.planning/phases/01-flucoma-descriptors/01-CONTEXT.md
@audioguide/tests.py
@audioguide/anallinkage.py
</context>

<tasks>

<task type="auto">
  <name>Add FLUCOMA option validation to tests.py</name>
  <files>audioguide/tests.py</files>
  <action>
Add validation for FLUCOMA configuration options in tests.py UserVar_types dictionary:

1. Add FLUCOMA_ENABLE validation: 'True or False'
2. Add FLUCOMA_DESCRIPTORS validation: 'a list of strings'  
3. Add FLUCOMA_PRESET validation: key in FLUCOMA_PRESETS or None
4. Add FLUCOMA_MFCC_COUNT validation: 'an integer greater than zero'
5. Add FLUCOMA_NORMALIZE validation: 'True or False'
6. Add FLUCOMA_CACHE_CORPUS validation: 'True or False'

See existing patterns in tests.py for 'True or False', 'a list of strings', etc.
</action>
  <verify>grep -n "FLUCOMA" audioguide/tests.py</verify>
  <done>All FLUCOMA_* options have validation in tests.py</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete FluCoMa descriptor integration - config, backend, presets, normalization</what-built>
  <how-to-verify>
    1. Create test script that:
       - Sets FLUCOMA_ENABLE = True
       - Sets FLUCOMA_PRESET = 'timbre'  
       - Loads a small corpus (2-3 audio files)
       - Computes descriptors for corpus
       - Checks that flucomamfcc1, flucomaspectral_centroid, flucomapitch exist in descriptor data
    
    2. Verify target can also use FluCoMa descriptors (same extraction, fresh computation)
    
    3. Verify corpus descriptors are cached (second run faster)
    
    4. Verify FluCoMa + IRCAM can be combined
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Run existing test suite to ensure no regressions
</verification>

<success_criteria>
- [ ] All FLUCOMA_* options validated in tests.py
- [ ] End-to-end test: FLUCOMA_ENABLE=True with corpus works
- [ ] End-to-end test: FLUCOMA_PRESET='timbre' works
- [ ] Human verification that FluCoMa descriptors appear in output
</success_criteria>

<output>
After completion, create `.planning/phases/01-flucoma-descriptors/01-03-SUMMARY.md`
</output>
