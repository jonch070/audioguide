---
phase: 02-reaper-takeenv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "TAKEENV is written to RPP file when ENABLE_TAKEENV=True"
    - "Per-item gain can be configured per-layer in config"
    - "TAKEENV format matches Reaper's expected structure"
  artifacts:
    - path: "audioguide/fileoutput/reaper.py"
      provides: "TAKEENV chunk writing in write() method"
      contains: "format_takeenv"
    - path: "audioguide/defaults.py"
      provides: "TAKEENV_PER_ITEM_GAIN config option"
      contains: "TAKEENV_PER_ITEM_GAIN"
  key_links:
    - from: "audioguide/__init__.py"
      to: "takeenv_processor.py"
      via: "process_tracks_for_takeenv call"
    - from: "reaper.py write()"
      to: "format_takeenv()"
      via: "takeenv_str = format_takeenv(...)"
---

<objective>
Enable TAKEENV writing to RPP files with per-item gain configuration.

Purpose: Complete the TAKEENV implementation by actually writing the TAKEENV chunk to RPP output (format_takeenv exists but isn't called), and add per-item gain configuration option.
</objective>

<context>
@audioguide/fileoutput/reaper.py
@audioguide/takeenv_processor.py
@audioguide/defaults.py
@audioguide/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Add TAKEENV_PER_ITEM_GAIN config option</name>
  <files>audioguide/defaults.py</files>
  <action>
Add new configuration option TAKEENV_PER_ITEM_GAIN (default 0.0) for per-item gain offset in dB. Add it near the existing TAKEENV_STATIC_GAIN option (around line 84-86). Register in tests.py.
  </action>
  <verify>grep "TAKEENV_PER_ITEM_GAIN" audioguide/defaults.py returns the new option</verify>
  <done>TAKEENV_PER_ITEM_GAIN config option exists with default 0.0</done>
</task>

<task type="auto">
  <name>Call format_takeenv in reaper.py write method</name>
  <files>audioguide/fileoutput/reaper.py</files>
  <action>
In the output.write() method, after building the item string but before closing the ITEM chunk, check if gain_envelope exists and format_takeenv() is available. Append the TAKEENV string to each item. The format_takeenv() function already exists but is never called - add the call inside the item loop (around line 119-132 where VOLPAN is set).

Key: The TAKEENV must go INSIDE the TAKE chunk, after the SOURCE. Looking at line 127-130, the SOURCE is added, then the item closes. Need to add TAKEENV between SOURCE and the closing > of ITEM.
  </action>
  <verify>Generated RPP contains "&lt;TAKEENV" strings when gain_envelope exists</verify>
  <done>TAKEENV automation chunks appear in RPP output</done>
</task>

<task type="auto">
  <name>Pass per-item gain to takeenv_processor</name>
  <files>audioguide/__init__.py</files>
  <action>
In the spectral_concatenate() method (around line 707-712 where ENABLE_TAKEENV is checked), pass TAKEENV_PER_ITEM_GAIN to the takeenv_processor. The takeenv_processor already receives static_gain_db - ensure it's connected to the new config option.
  </action>
  <verify>grep "TAKEENV_PER_ITEM_GAIN" audioguide/__init__.py returns the config usage</verify>
  <done>Per-item gain value flows from config to takeenv_processor</done>
</task>

</tasks>

<verification>
1. Run a test that generates RPP with ENABLE_TAKEENV=True
2. Check the RPP file contains "&lt;TAKEENV" chunks
3. Verify automation points appear in correct format
</verification>

<success_criteria>
- [x] TAKEENV written to RPP when enabled
- [x] Per-item gain config option available
- [x] format_takeenv() is called in write()
</success_criteria>

<output>
After completion, create .planning/phases/02-reaper-takeenv/02-01-SUMMARY.md
</output>
