---
phase: 03-synthesis-upgrades
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - audioguide/defaults.py
  - audioguide/fileoutput/midi.py (new)
  - audioguide/spectrallayering.py
  - audioguide/__init__.py
autonomous: true

must_haves:
  truths:
    - "User can export results as MIDI file"
    - "Stochastic corpus selection adds natural variation"
    - "MIDI output includes pitch, velocity, and timing"
  artifacts:
    - path: "audioguide/fileoutput/midi.py"
      provides: "MIDI file output for Reaper/DAW integration"
      exports: ["write_midi_file", "MIDIEvent"]
    - path: "audioguide/fileoutput/__init__.py"
      via: "Export midi module"
      pattern: "midi"
key_links:
  - from: "midi.py"
    to: "spectrallayering.py"
    via: "Uses output events for MIDI generation"
    pattern: "outputEvents"
---

<objective>
Implement MIDI output format and stochastic corpus selection

Purpose: MIDI output enables integration with DAWs and virtual instruments.
Stochastic selection adds natural variation to prevent robotic-sounding results.

Output: midi.py with MIDI file generation, enhanced spectrallayering.py with stochastic mode
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</execution_context>

<context>
@audioguide/defaults.py
@audioguide/fileoutput/reaper.py
@audioguide/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MIDI config options</name>
  <files>audioguide/defaults.py</files>
  <action>
Add MIDI output options to defaults.py:

1. MIDI_FILEPATH (str): Output path for MIDI file
2. MIDI_CHANNEL (int): MIDI channel 1-16 (default 1)
3. MIDI_VELOCITY_SOURCE (str): 'target_loudness', 'corpus_velocity', 'fixed'
4. MIDI_VELOCITY_FIXED (int): Fixed velocity 0-127 (default 100)
5. MIDI_TRANSPOSE_OCTAVES (int): Transpose in octaves (default 0)
6. MIDI_INSTRUMENT (str): GM instrument name or program number

Add stochastic selection options:
7. STOCHASTIC_SELECTION (bool): Enable stochastic corpus selection
8. STOCHASTIC_TEMPERATURE (float): Temperature for softmax (default 1.0)
9. STOCHASTIC_TOP_K (int): Consider top K matches (default 1)
10. STOCHASTIC_DIVERSITY_PENALTY (float): Penalize repeated corpus files
</action>
  <verify>python -c "from audioguide import defaults; print('MIDI_FILEPATH:', hasattr(defaults, 'MIDI_FILEPATH')); print('STOCHASTIC:', hasattr(defaults, 'STOCHASTIC_SELECTION'))"</verify>
  <done>MIDI and stochastic config options available</done>
</task>

<task type="auto">
  <name>Task 2: Implement MIDI output module</name>
  <files>audioguide/fileoutput/midi.py</files>
  <action>
Create audioguide/fileoutput/midi.py with:

1. MIDIEvent class:
   - pitch: MIDI note number (0-127)
   - velocity: MIDI velocity (0-127)
   - start_time_sec: Start time in seconds
   - duration_sec: Note duration
   - channel: MIDI channel (1-16)

2. write_midi_file() function:
   - events: List of MIDIEvent objects
   - filepath: Output .mid file path
   - Writes Standard MIDI File format

3. Helper functions:
   - freq_to_midi(freq): Convert frequency to MIDI note
   - midi_to_freq(midi): Convert MIDI note to frequency
   - velocity_from_loudness(db): Convert dB to velocity

4. create_midi_from_output_events() function:
   - Converts AudioGuide outputEvents to MIDIEvent list
   - Applies velocity and transpose settings
</action>
  <verify>python -c "from audioguide.fileoutput.midi import MIDIEvent, write_midi_file; e = MIDIEvent(60, 100, 0.0, 1.0); print('MIDI event:', e.pitch)"</verify>
  <done>MIDI module created and functional</done>
</task>

<task type="auto">
  <name>Task 3: Implement stochastic corpus selection</name>
  <files>audioguide/spectrallayering.py</files>
  <action>
Enhance spectrallayering.py with stochastic selection:

1. Add stochastic_candidates() function:
   - Takes list of candidate corpus segments with scores
   - Applies softmax with temperature
   - Returns probabilistically selected candidate

2. Add diversity_penalty() function:
   - Penalizes recently used corpus files
   - Encourages diverse corpus usage

3. Modify spectral_layering_match():
   - When STOCHASTIC_SELECTION=True
   - Use stochastic candidate selection
   - Apply temperature and diversity penalty
</action>
  <verify>python -c "
from audioguide import defaults
defaults.STOCHASTIC_SELECTION = True
defaults.STOCHASTIC_TEMPERATURE = 0.5
print('Stochastic selection enabled')
"</verify>
  <done>Stochastic selection implemented</done>
</task>

<task type="auto">
  <name>Task 4: Wire MIDI into __init__.py output</name>
  <files>audioguide/__init__.py</files>
  <action>
Integrate MIDI output into AudioGuide:

1. Add MIDI file generation to write_concatenate_output_files()
2. Call write_midi_file() when MIDI_FILEPATH is set
3. Convert outputEvents to MIDIEvent list
4. Handle timing and duration from corpus segments

5. Add to fileoutput/__init__.py:
   - Export midi module
</action>
  <verify>python -c "from audioguide.fileoutput import midi; print('MIDI module exported')"</verify>
  <done>MIDI output wired into main workflow</done>
</task>

</tasks>

<verification>
- MIDI file is created when MIDI_FILEPATH is set
- MIDI notes correspond to corpus segment pitches
- Stochastic selection produces varied results
- Temperature affects selection randomness
</verification>

<success_criteria>
- RPP output includes MIDI trigger track when enabled
- STOCHASTIC_SELECTION=True picks different corpus files
- MIDI plays correct notes in DAW
</success_criteria>

<output>
After completion, create .planning/phases/03-synthesis-upgrades/03-02-SUMMARY.md
</output>
