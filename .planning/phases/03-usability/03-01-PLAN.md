---
phase: 03-usability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audioguide/defaults.py
  - audioguide/__init__.py
  - audioguide/userclasses.py
autonomous: true

must_haves:
  truths:
    - "User can create project from 'single note', 'melody', or 'chord' template"
    - "User can save configuration to JSON file"
    - "User can load configuration from JSON file"
    - "User can run validation before execution and see all issues listed"
  artifacts:
    - path: "audioguide/templates.py"
      provides: "Template definitions for single note, melody, chord"
      exports: ["get_template", "list_templates", "TEMPLATES"]
    - path: "audioguide/config_io.py"
      provides: "JSON config save/load functionality"
      exports: ["save_config_to_json", "load_config_from_json"]
    - path: "audioguide/validator.py"
      provides: "Config validation with detailed error messages"
      exports: ["validate_config", "ConfigValidationError"]
  key_links:
    - from: "templates.py"
      to: "defaults.py"
      via: "template applies DEFAULT settings"
      pattern: "USE_SPECTRAL_RECONSTRUCTION.*="
    - from: "config_io.py"
      to: "validator.py"
      via: "load_config runs validation"
      pattern: "validate_config"
---

<objective>
Create template system, JSON config save/load, and config validation

Purpose: Foundation for usability improvements - templates let users get started quickly, JSON config enables persistence, validation prevents runtime errors

Output: templates.py, config_io.py, validator.py with working save/load/validate
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@audioguide/defaults.py
@audioguide/tests.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create template system</name>
  <files>audioguide/templates.py</files>
  <action>
Create audioguide/templates.py with:

1. TEMPLATES dictionary containing three templates:
   - "single_note": USE_SPECTRAL_RECONSTRUCTION=True, SPECTRAL_WHOLE_FILE=True, SPECTRAL_MAX_PARTIALS=8
   - "melody": USE_SPECTRAL_RECONSTRUCTION=True, SPECTRAL_WHOLE_FILE=False, SPECTRAL_MAX_PARTIALS=4
   - "chord": USE_SPECTRAL_RECONSTRUCTION=True, SPECTRAL_WHOLE_FILE=False, SPECTRAL_MAX_PARTIALS=24

2. get_template(name) function that returns template dict or raises ValueError
3. list_templates() function that returns available template names
4. apply_template(template_name) function that sets defaults module variables

Reference: spectral settings are in defaults.py lines 113-125
  </action>
  <verify>python -c "from audioguide.templates import get_template, list_templates; print(list_templates()); t = get_template('single_note'); print(t['SPECTRAL_MAX_PARTIALS'])"</verify>
  <done>Template system works: get_template('single_note') returns dict with SPECTRAL_WHOLE_FILE=True, get_template('melody') returns dict with SPECTRAL_MAX_PARTIALS=4, get_template('chord') returns dict with SPECTRAL_MAX_PARTIALS=24</done>
</task>

<task type="auto">
  <name>Task 2: Create JSON config save/load</name>
  <files>audioguide/config_io.py</files>
  <action>
Create audioguide/config_io.py with:

1. save_config_to_json(filepath) function:
   - Collects all uppercase variables from defaults module
   - Converts to JSON-serializable format (handle tsf(), csf() objects specially)
   - Writes to specified filepath

2. load_config_from_json(filepath) function:
   - Reads JSON file
   - Validates using validator (Task 3)
   - Applies settings to defaults module
   - Returns dict of loaded config

3. _serialize_option() helper to handle special types
4. _deserialize_option() helper to restore special types

Handle these special types: tsf() object, csf() object, si() object, list, dict, None
  </action>
  <verify>python -c "from audioguide import defaults; from audioguide.config_io import save_config_to_json, load_config_from_json; save_config_to_json('/tmp/test_config.json'); print('Saved'); import json; print(json.load(open('/tmp/test_config.json')).keys())"</verify>
  <done>JSON config save works: save_config_to_json() creates valid JSON file with all config options, load_config_from_json() restores settings correctly</done>
</task>

<task type="auto">
  <name>Task 3: Create config validation</name>
  <files>audioguide/validator.py</files>
  <action>
Create audioguide/validator.py with:

1. ConfigValidationError class:
   - Inherits from Exception
   - Has attributes: errors (list of {field, message, suggestion})
   - Has __str__ that formats all errors nicely

2. validate_config(config_dict) function:
   - Validates all config options against tests.py UserVar_types
   - Returns list of issues (not just first error)
   - Each issue: {field, message, suggestion}

3. validate_config_file(filepath) function:
   - Loads JSON from filepath
   - Runs validation
   - Returns ConfigValidationError if issues found

4. Built-in validation rules for common issues:
   - TARGET must be tsf() object or path string
   - CORPUS must be list of csf() objects or path strings  
   - SPECTRAL_MAX_PARTIALS must be positive integer
   - File paths must exist (if specified)
  </action>
  <verify>python -c "from audioguide.validator import validate_config, ConfigValidationError; err = validate_config({'SPECTRAL_MAX_PARTIALS': -1}); print(len(err.errors) > 0)"</verify>
  <done>Config validation works: validate_config({'SPECTRAL_MAX_PARTIALS': -1}) returns ConfigValidationError with field='SPECTRAL_MAX_PARTIALS' and suggestion='must be positive integer'</done>
</task>

</tasks>

<verification>
- All three modules import without errors
- Template system returns correct defaults for each template type
- JSON save/load preserves all config options
- Validation catches invalid values and provides suggestions
</verification>

<success_criteria>
- Templates available via get_template('single_note'|'melody'|'chord')
- JSON config can be saved and loaded
- validate_config() returns all issues (not just first)
</success_criteria>

<output>
After completion, create .planning/phases/03-usability/03-01-SUMMARY.md
</output>
