---
phase: 03-usability
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - audioguide/__init__.py
  - audioguide/util.py
  - audioguide/cli.py
autonomous: true

must_haves:
  truths:
    - "User can run AudioGuide from command line without starting web GUI"
    - "When configuration is invalid, user sees message explaining what's wrong and how to fix"
  artifacts:
    - path: "audioguide/cli.py"
      provides: "Command-line interface for headless execution"
      exports: ["main", "parse_args"]
    - path: "audioguide/util.py"
      via: "Enhanced error() function with fix suggestions"
      pattern: "def error.*suggestion"
  key_links:
    - from: "cli.py"
      to: "validator.py"
      via: "Runs validation before execution"
      pattern: "validate_config"
    - from: "cli.py"
      to: "templates.py"
      via: "Supports --template flag"
      pattern: "apply_template"
---

<objective>
Implement headless CLI mode and improve error messages

Purpose: Users can run AudioGuide from terminal without GUI, and when errors occur they get actionable fix suggestions

Output: cli.py with argparse-based CLI, enhanced util.error() with suggestions
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-usability/03-01-SUMMARY.md
@audioguide/__init__.py
@audioguide/util.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create headless CLI entry point</name>
  <files>audioguide/cli.py</files>
  <action>
Create audioguide/cli.py with argparse-based command-line interface:

1. parse_args() function that handles:
   - --config/-c: JSON config file path
   - --template/-t: Template name (single_note, melody, chord)
   - --validate-only: Run validation only, don't execute
   - --target/-i: Target audio file path
   - --corpus/-j: Corpus directory path
   - --output/-o: Output path
   - --verbose/-v: Verbose output
   - --version: Show version

2. main() function that:
   - Parses arguments
   - Loads template if specified
   - Loads config file if specified
   - Runs validation (from validator.py)
   - If validation passes, runs concatenate()
   - If validation fails, prints all errors and exits

3. Entry point: if __name__ == '__main__': main()

4. Support config file comments explaining each option
  </action>
  <verify>python -c "from audioguide.cli import parse_args; import sys; sys.argv = ['ag', '--help']; parse_args()" 2>&1 | head -20</verify>
  <done>CLI works: python -m audioguide --help shows all options, --validate-only checks config without running</done>
</task>

<task type="auto">
  <name>Task 2: Enhance error messages with suggestions</name>
  <files>audioguide/util.py</files>
  <action>
Enhance audioguide/util.py error() function:

1. Add ERROR_SUGGESTIONS dictionary mapping common errors to fix suggestions:
   - "must be a tsf() object" -> "Create with targetfile('path/to/audio.wav', ...)"
   - "must be a list of csf() objects" -> "Create with corpusfile('path/to/dir', ...)"
   - "must be a number" -> "Remove quotes around the value"
   - "file not found" -> "Check the file path is correct"
   - "no segments found" -> "Check target file is not silent, adjust thresh parameter"

2. Enhance error() function:
   - Accept optional 'suggestion' parameter
   - If errorData matches known patterns, append suggestion
   - Format: "ERROR: {message}\n  Suggestion: {fix}"

3. Add util.config_error() function for config-specific errors:
   - Takes field name and validation message
   - Looks up suggestion from ERROR_SUGGESTIONS
   - Prints formatted error with suggestion
  </action>
  <verify>python -c "from audioguide.util import error; error('CONFIG', 'SPECTRAL_MAX_PARTIALS must be a number greater than zero', suggestion='Set SPECTRAL_MAX_PARTIALS to a positive integer like 8')"</verify>
  <done>Error messages improved: error() now shows suggestion when provided, format is "ERROR: {msg}\n  Suggestion: {fix}"</done>
</task>

<task type="auto">
  <name>Task 3: Wire CLI into __init__.py</name>
  <files>audioguide/__init__.py</files>
  <action>
Update audioguide/__init__.py to support CLI:

1. Add entry point function that can be called from command line:
   - Check if __name__ == '__main__' or sys.argv has CLI flags
   - Import and call cli.main() if CLI mode detected

2. Make concatenate() function directly callable:
   - Keep existing behavior when called as script
   - Allow import and call from cli.py

3. Add --cli flag to force CLI mode (bypass Flask detection)

4. Register cli module for package entry point
  </action>
  <verify>python -c "import audioguide; print(hasattr(audioguide, 'concatenate'))"</verify>
  <done>CLI wired: python -m audioguide --help works, concatenate() remains callable as before</done>
</task>

</tasks>

<verification>
- CLI responds to --help, --validate-only, --config, --template
- Error messages include suggestions when available
- Validation errors show all issues before execution
</verification>

<success_criteria>
- Headless mode: `python -m audioguide --config config.json` runs without GUI
- Validation: `python -m audioguide --validate-only --config config.json` shows all issues
- Errors: Invalid config shows field name, what's wrong, and how to fix
</success_criteria>

<output>
After completion, create .planning/phases/03-usability/03-02-SUMMARY.md
</output>
