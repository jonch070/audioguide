---
phase: 04-performance-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audioguide/defaults.py
  - audioguide/descriptor_backends.py
  - audioguide/util.py
autonomous: true

must_haves:
  truths:
    - "Descriptor calculation runs on multiple CPU cores"
    - "Analysis results are cached for reuse"
    - "Cache can be cleared manually"
  artifacts:
    - path: "audioguide/cache.py"
      provides: "Descriptor caching system"
      exports: ["DescriptorCache", "get_cached", "set_cached", "clear_cache"]
    - path: "audioguide/parallel.py"
      provides: "Parallel processing utilities"
      exports: ["parallel_map", "ThreadPool"]
key_links:
  - from: "cache.py"
    to: "descriptor_backends.py"
    via: "Cache analysis results"
    pattern: "analyze_file"
---

<objective>
Implement parallel descriptor calculation and caching system

Purpose: Speed up analysis by using multiple CPU cores and avoid re-analyzing
corpus files by caching descriptor results.

Output: cache.py with caching, parallel.py with thread pool
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</execution_context>

<context>
@audioguide/defaults.py
@audioguide/descriptor_backends.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parallel and cache config options</name>
  <files>audioguide/defaults.py</files>
  <action>
Add performance config options:

1. PARALLEL_DESCRIPTORS (bool): Enable parallel processing
2. PARALLEL_NUM_WORKERS (int): Number of worker threads (default: CPU count)
3. CACHE_DESCRIPTORS (bool): Enable descriptor caching
4. CACHE_DIR (str): Cache directory path
5. CACHE_MAX_SIZE_GB (float): Max cache size in GB
6. CACHE_TTL_DAYS (int): Cache time-to-live in days
7. BATCH_PROCESSING (bool): Enable batch mode
8. BATCH_INPUT_DIR (str): Directory for batch input files
9. BATCH_OUTPUT_DIR (str): Directory for batch output files
</action>
  <verify>python -c "from audioguide import defaults; print('PARALLEL:', hasattr(defaults, 'PARALLEL_DESCRIPTORS')); print('CACHE:', hasattr(defaults, 'CACHE_DESCRIPTORS'))"</verify>
  <done>Performance config options available</done>
</task>

<task type="auto">
<parameter name="Task 2: Create caching module</name>
  files=audioguide/cache.py
  <action>
Create audioguide/cache.py:

1. DescriptorCache class:
   - __init__(cache_dir, max_size_gb=10, ttl_days=30)
   - get(key): Retrieve cached value
   - set(key, value): Store value in cache
   - exists(key): Check if key exists
   - clear(): Clear all cache
   - clear_expired(): Remove expired entries
   
2. Cache key generation:
   - compute_key(file_path, analysis_params): MD5 hash of path + params
   
3. Persistence:
   - Store as JSON files with metadata
   - Automatic cleanup of old entries

4. Thread-safe operations using locks
</action>
  <verify>python -c "from audioguide.cache import DescriptorCache; c = DescriptorCache('/tmp/test_cache'); c.clear(); print('Cache works')"</verify>
  <done>Caching module created</done>
</task>

<task type="auto">
  <name>Task 3: Create parallel processing module</name>
  <files>audioguide/parallel.py</files>
  <action>
Create audioguide/parallel.py:

1. parallel_map() function:
   - Apply function to list in parallel
   - Uses ThreadPoolExecutor or ProcessPoolExecutor
   - Returns list of results in same order
   
2. analyze_corpus_parallel() function:
   - Analyze corpus files in parallel
   - Progress callback support
   - Handles errors gracefully

3. ThreadPool/ProcessPool classes:
   - Configurable worker count
   - Progress tracking
   - Cancellation support
</action>
  <verify>python -c "from audioguide.parallel import parallel_map; result = parallel_map(lambda x: x*2, [1,2,3], workers=2); print('Result:', result)"</verify>
  <done>Parallel module created</done>
</task>

<task type="auto">
  <name>Task 4: Wire caching into descriptor backends</name>
  <files>audioguide/descriptor_backends.py</files>
  <action>
Integrate caching into descriptor computation:

1. Check cache before running analysis
2. Store results after analysis
3. Add cache bypass option

4. Add to FLUCOMA backend:
   - Cache FluCoMa analysis results
   
5. Add to IRCAM backend:
   - Cache IRCAM descriptor results
</action>
  <verify>python -c "from audioguide import defaults; defaults.CACHE_DESCRIPTORS = True; print('Cache enabled')"</verify>
  <done>Caching wired into backends</done>
</task>

<task type="auto">
  <name>Task 5: Add cache CLI commands</name>
  <files>audioguide/cli.py</files>
  <action>
Add cache management to CLI:

1. --clear-cache: Clear descriptor cache
2. --cache-stats: Show cache statistics
3. --parallel/--no-parallel: Override parallel setting
</action>
  <verify>python3 -m audioguide --help | grep -i cache</verify>
  <done>Cache CLI commands added</done>
</task>

</tasks>

<verification>
- Cache stores and retrieves descriptor data correctly
- Parallel processing speeds up multi-file analysis
- Cache can be cleared via CLI
</verification>

<success_criteria>
- PARALLEL_DESCRIPTORS=True uses multiple cores
- CACHE_DESCRIPTORS=True caches analysis results
- --clear-cache removes cached data
</success_criteria>

<output>
After completion, create .planning/phases/04-performance-workflow/04-01-SUMMARY.md
</output>
