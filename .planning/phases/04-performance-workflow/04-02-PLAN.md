---
phase: 04-performance-workflow
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - audioguide/defaults.py
  - audioguide/batch.py (new)
  - audioguide/cli.py
autonomous: true

must_haves:
  truths:
    - "User can process multiple files in batch mode"
    - "Batch results are organized in output directory"
    - "Batch progress is reported"
  artifacts:
    - path: "audioguide/batch.py"
      provides: "Batch processing functionality"
      exports: ["BatchProcessor", "process_batch"]
key_links:
  - from: "batch.py"
    to: "cli.py"
    via: "Batch mode CLI"
    pattern: "batch"
---

<objective>
Implement batch processing for processing multiple files

Purpose: Allow users to process multiple target files against a corpus
in one go, with results organized by file.

Output: batch.py with batch processing
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</execution_context>

<context>
@audioguide/defaults.py
@audioguide/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add batch config options</name>
  <files>audioguide/defaults.py</files>
  <action>
Add batch processing options:

1. BATCH_ENABLE (bool): Enable batch mode
2. BATCH_INPUT_DIR (str): Directory containing target files
3. BATCH_OUTPUT_DIR (str): Output directory for results
4. BATCH_PATTERN (str): Glob pattern for input files (*.wav)
5. BATCH_CONTINUE_ON_ERROR (bool): Continue if one file fails
6. BATCH_SUMMARY_FILE (str): JSON summary file path
</action>
  <verify>python -c "from audioguide import defaults; print('BATCH:', hasattr(defaults, 'BATCH_ENABLE'))"</verify>
  <done>Batch config options added</done>
</task>

<task type="auto">
  <name>Task 2: Create batch processing module</name>
  <files>audioguide/batch.py</files>
  <action>
Create audioguide/batch.py:

1. BatchProcessor class:
   - __init__(input_dir, output_dir, config)
   - scan_input_files(): Find files to process
   - process_all(): Process all files
   - process_single(file_path): Process one file
   
2. process_batch() function:
   - Main entry point for batch processing
   - Handles parallel execution if enabled
   - Reports progress
   
3. BatchResult class:
   - file_path: Input file
   - success: bool
   - output_files: List of output paths
   - error: Error message if failed
   - duration_sec: Processing time
   
4. BatchSummary:
   - total_files: int
   - successful: int
   - failed: int
   - results: List[BatchResult]
</action>
  <verify>python -c "from audioguide.batch import BatchProcessor, process_batch; print('Batch module OK')"</verify>
  <done>Batch module created</done>
</task>

<task type="auto">
  <name>Task 3: Add batch CLI commands</name>
  <files>audioguide/cli.py</files>
  <action>
Add batch mode to CLI:

1. --batch: Enable batch mode
2. --batch-input: Input directory
3. --batch-output: Output directory
4. --batch-pattern: File pattern (default *.wav)

5. Update main() to handle batch mode
6. Print summary after batch completes
</action>
  <verify>python3 -m audioguide --help | grep -i batch</verify>
  <done>Batch CLI commands added</done>
</task>

<task type="auto">
  <name>Task 4: Add corpus optimization suggestions</name>
  <files>audioguide/batch.py</files>
  <action>
Add corpus analysis for suggestions:

1. analyze_corpus_coverage() function:
   - Check which frequencies the corpus covers
   - Identify gaps in coverage
   
2. suggest_corpus_improvements() function:
   - Suggest corpus additions for better coverage
   - Based on target file analysis
   
3. Include suggestions in batch summary
</action>
  <verify>python -c "from audioguide.batch import analyze_corpus_coverage; print('Corpus analysis OK')"</verify>
  <done>Corpus suggestions added</done>
</task>

</tasks>

<verification>
- Batch processes multiple files
- Results organized in output directory
- Summary JSON created
</verification>

<success_criteria>
- --batch processes all files in input directory
- Output directory contains results for each file
- Summary shows success/failure count
</success_criteria>

<output>
After completion, create .planning/phases/04-performance-workflow/04-02-SUMMARY.md
</output>
