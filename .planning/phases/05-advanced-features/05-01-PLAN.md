---
phase: 05-advanced-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - audioguide/defaults.py
  - audioguide/ml.py (new)
  - audioguide/spectrallayering.py
autonomous: true

must_haves:
  truths:
    - "User can enable neural network timbre matching"
    - "Model can be trained on corpus for custom timbre matching"
    - "Inference runs efficiently on CPU"
  artifacts:
    - path: "audioguide/ml.py"
      provides: "ML-based timbre matching and parameter optimization"
      exports: ["TimbreMatcher", "train_timbre_model", "infer_timbre"]
    - path: "audioguide/defaults.py"
      via: "Add ML config options"
      pattern: "ML_"
key_links:
  - from: "ml.py"
    to: "spectrallayering.py"
    via: "Use ML model for matching"
    pattern: "spectral_"
---

<objective>
Implement neural network timbre matching and ML-based features

Purpose: Use machine learning for better timbre matching than descriptor-based approaches.
Train models on corpus for custom sound matching.

Output: ml.py with TimbreMatcher class
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</execution_context>

<context>
@audioguide/defaults.py
@audioguide/spectrallayering.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ML config options</name>
  <files>audioguide/defaults.py</files>
  <action>
Add ML and neural network options:

1. ML_ENABLE (bool): Enable ML-based timbre matching
2. ML_MODEL_PATH (str): Path to trained model file
3. ML_TRAIN_ON_CORPUS (bool): Train model on corpus before matching
4. ML_MODEL_TYPE (str): 'simple_nn', 'autoencoder', 'contrastive'
5. ML_EMBEDDING_DIM (int): Dimension of timbre embedding (default 128)
6. ML_HIDDEN_DIM (int): Hidden layer dimension (default 256)
7. ML_EPOCHS (int): Training epochs (default 100)
8. ML_BATCH_SIZE (int): Training batch size (default 32)
9. ML_LEARNING_RATE (float): Learning rate (default 0.001)
10. STYLE_TRANSFER_ENABLE (bool): Enable style transfer mode
</action>
  <verify>python -c "from audioguide import defaults; print('ML_ENABLE:', hasattr(defaults, 'ML_ENABLE'))"</verify>
  <done>ML config options available</done>
</task>

<task type="auto">
<name>Task 2: Create ML module</name>
  <files>audioguide/ml.py</files>
  <action>
Create audioguide/ml.py with:

1. SimpleTimbreNN class:
   - Simple feedforward network for timbre embedding
   - Uses spectral features as input
   - Outputs embedding vector

2. TimbreMatcher class:
   - load_model(path): Load trained model
   - train(corpus_segments): Train on corpus
   - match(target_embedding, corpus_embeddings): Find best matches
   - extract_embedding(audio): Extract embedding from audio

3. train_timbre_model() function:
   - Train model on corpus segments
   - Use contrastive learning for better matching

4. infer_timbre() function:
   - Quick inference for matching

5. FeatureExtractor:
   - Extract spectral features from audio
   - MFCCs, spectral centroid, etc.
</action>
  <verify>python -c "from audioguide.ml import SimpleTimbreNN, TimbreMatcher; print('ML module OK')"</verify>
  <done>ML module created</done>
</task>

<task type="auto">
<name>Task 3: Add style transfer support</name>
  <files>audioguide/ml.py</files>
  <action>
Add style transfer capabilities:

1. StyleTransfer class:
   - extract_style(audio): Extract style features
   - transfer_style(content, style): Apply style to content
   
2. corpus_style_analysis() function:
   - Analyze style of entire corpus
   - Return style profile
   
3. style_match() function:
   - Match target style to corpus style

4. Add to defaults:
   - STYLE_TRANSFER_ENABLE
   - STYLE_STRENGTH (0-1)
</action>
  <verify>python -c "from audioguide.ml import StyleTransfer; print('Style transfer OK')"</verify>
  <done>Style transfer added</done>
</task>

<task type="auto">
  <name>Task 4: Wire ML into spectral workflow</name>
  <files>audioguide/spectrallayering.py</files>
  <action>
Integrate ML matching:

1. Add ML match mode to spectral_layering_match()
2. When ML_ENABLE=True, use TimbreMatcher
3. Fall back to descriptor matching if ML fails

4. Add parameter optimization:
   - optimize_parameters() - Auto-tune config based on target
</action>
  <verify>python -c "from audioguide import defaults; defaults.ML_ENABLE = True; print('ML enabled')"</verify>
  <done>ML wired into workflow</done>
</task>

</tasks>

<verification>
- SimpleTimbreNN can be instantiated
- TimbreMatcher can train on dummy data
- Style transfer extracts features
</verification>

<success_criteria>
- ML_ENABLE=True uses neural network matching
- ML_MODEL_PATH loads external model
- STYLE_TRANSFER_ENABLE applies style
</success_criteria>

<output>
After completion, create .planning/phases/05-advanced-features/05-01-SUMMARY.md
</output>
