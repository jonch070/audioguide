---
phase: 05-advanced-features
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - audioguide/defaults.py
  - audioguide/realtime.py (new)
  - audioguide/cli.py
autonomous: true

must_haves:
  truths:
    - "User can process live audio input"
    - "Audio streams to output with minimal latency"
    - "Parameters can be adjusted in real-time"
  artifacts:
    - path: "audioguide/realtime.py"
      provides: "Live input and real-time processing"
      exports: ["RealtimeProcessor", "StreamInput", "process_live"]
key_links:
  - from: "realtime.py"
    to: "spectrallayering.py"
    via: "Live spectral matching"
    pattern: "spectral_"
---

<objective>
Implement live input processing and real-time capabilities

Purpose: Allow live audio input processing with low latency, enabling
real-time concatenative synthesis performances.

Output: realtime.py with streaming audio processing
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</execution_context>

<context>
@audioguide/defaults.py
@audioguide/spectrallayering.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add real-time config options</name>
  <files>audioguide/defaults.py</files>
  <action>
Add real-time processing options:

1. REALTIME_ENABLE (bool): Enable real-time mode
2. REALTIME_BUFFER_SIZE (int): Audio buffer size samples (default 512)
3. REALTIME_HOP_SIZE (int): Processing hop size (default 256)
4. REALTIME_LATENCY_TARGET_MS (int): Target latency in ms (default 20)
5. REALTIME_INPUT_DEVICE (int): Audio input device index
6. REALTIME_OUTPUT_DEVICE (int): Audio output device index
7. REALTIME_USE_JACK (bool): Use JACK audio instead of PortAudio
8. MIDI_CONTROL_ENABLE (bool): Enable MIDI controller input
9. MIDI_CONTROLLER_MAPPING (dict): MIDI CC to parameter mapping
</action>
  <verify>python -c "from audioguide import defaults; print('REALTIME:', hasattr(defaults, 'REALTIME_ENABLE'))"</verify>
  <done>Real-time config options available</done>
</task>

<task type="auto">
<name>Task 2: Create realtime module</name>
  <files>audioguide/realtime.py</files>
  <action>
Create audioguide/realtime.py:

1. AudioStreamHandler class:
   - Manages input/output audio streams
   - Uses sounddevice or JACK
   - Handles buffer management
   
2. RealtimeProcessor class:
   - Processes audio in real-time
   - Spectral analysis on incoming audio
   - Matches to corpus on-the-fly
   
3. process_live() function:
   - Main entry point for live processing
   - Sets up audio streams
   - Runs processing loop

4. LatencyTracker class:
   - Monitors processing latency
   - Adaptive buffer sizing

5. ParameterController class:
   - Real-time parameter adjustment
   - MIDI controller support
</action>
  <verify>python -c "from audioguide.realtime import RealtimeProcessor, AudioStreamHandler; print('Realtime module OK')"</verify>
  <done>Realtime module created</done>
</task>

<task type="auto">
<name>Task 3: Add MIDI controller support</name>
  <files>audioguide/realtime.py</files>
  <action>
Enhance realtime.py with MIDI:

1. MIDIInputHandler class:
   - Read MIDI input from controller
   - Map CC messages to parameters
   
2. Built-in mappings:
   - CC 1 (Mod Wheel): Master volume
   - CC 7 (Volume): Output gain
   - CC 11 (Expression): Spectral mix
   - Pitch bend: Transposition

3. MIDI learn mode:
   - Assign MIDI to any parameter
   - Save/load mappings
</action>
  <verify>python -c "from audioguide.realtime import MIDIInputHandler; print('MIDI handler OK')"</verify>
  <done>MIDI controller support added</done>
</task>

<task type="auto">
  <name>Task 4: Add CLI real-time commands</name>
  <files>audioguide/cli.py</files>
  <action>
Add real-time CLI commands:

1. --realtime: Enable real-time mode
2. --input-device: Specify input device
3. --output-device: Specify output device
4. --midi: Enable MIDI controller
5. --latency: Set target latency

6. Add 'live' subcommand:
   audioguide live --corpus corpus/ --realtime
</action>
  <verify>python3 -m audioguide --help | grep -i realtime</verify>
  <done>Real-time CLI commands added</done>
</task>

</tasks>

<verification>
- AudioStreamHandler can be instantiated
- RealtimeProcessor initialized
- MIDI handler receives input
</verification>

<success_criteria>
- REALTIME_ENABLE=True starts live processing
- Audio flows from input to output
- MIDI controls parameters in real-time
</success_criteria>

<output>
After completion, create .planning/phases/05-advanced-features/05-02-SUMMARY.md
</output>
